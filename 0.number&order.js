/*
Order of Operation in JavaScript
    * / are done first.
    + - are done after.

    this order of operation in javacript is 
    called Operation Precedence.

*/

/*
    + - have same priority. In this case, Computer will 
        calculate from left to right.

    Similary, 
    
    * / have same priority.In this case, Computer will 
        calculate from left to right also.

*/

// Math.round(); is use to convert floating point number to the nearest integer
console.log(Math.round(2.2));
console.log(Math.round(2.8));

//Math.random() is use to get random number.
console.log(Math.random());